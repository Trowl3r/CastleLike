import GameScene from "./GameScene.js";

// TODO: Take the registers into a collision register file

// TODO: Add collision to the attacks, that they get removed from the state on collision
// Or when they are outside the screen
new GameScene().start();
